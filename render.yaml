services:
  - type: web
    name: osint-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python osint_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
